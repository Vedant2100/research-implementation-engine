/**
 * Starter content — generated by scripts/gen-seed.mjs.
 * Do not edit by hand. To add or change an assignment:
 *   1. Edit or add Python files in scripts/assignments/
 *   2. Edit ASSIGNMENTS / PAPERS in scripts/gen-seed.mjs
 *   3. Bump SEED_VERSION in scripts/gen-seed.mjs
 *   4. Run: npm run gen-seed
 */

export const SEED_VERSION = "llm_week_v1";

export const SEED_PAPERS = [
    {
      "title": "An Image is Worth 16x16 Words: Transformers for Image Recognition at Scale",
      "authors": "Dosovitskiy et al.",
      "year": 2020,
      "venue": "ICLR 2021",
      "area": "llm",
      "core_idea": "Treat an image as a sequence of patches, embed each patch, run them through a standard transformer encoder with a learnable [CLS] token, and classify from the [CLS] output.",
      "why_important": "ViT is the canonical full-architecture transformer outside of language. Building it end-to-end teaches patching, positional encoding, multi-head self-attention, residual transformer blocks, and an entire training/eval pipeline in one project.",
      "pytorch_hook": "nn.Conv2d patch embedding + learnable [CLS] + positional embedding + stacked TransformerBlocks + classifier head + full training loop with evaluation"
    },
    {
      "title": "Language Models are Unsupervised Multitask Learners",
      "authors": "Radford et al.",
      "year": 2019,
      "venue": "OpenAI tech report",
      "area": "llm",
      "core_idea": "A decoder-only causal transformer trained on next-token prediction at scale is enough to learn surprising amounts of language understanding without task-specific supervision.",
      "why_important": "The GPT-2 architecture is the backbone every modern LLM iterates on. Reproducing it from scratch teaches token embeddings, causal masks, residual transformer blocks, and the LM head.",
      "pytorch_hook": "TokenEmbedding + positional embedding + causal self-attention + MLP + TransformerBlock + LM head + cross-entropy on next token"
    },
    {
      "title": "LLaMA: Open and Efficient Foundation Language Models",
      "authors": "Touvron et al.",
      "year": 2023,
      "venue": "arXiv preprint",
      "area": "llm",
      "core_idea": "Combine RoPE positional embeddings, RMSNorm, SwiGLU MLPs, and (in later variants) grouped-query attention into a cleaner, faster decoder-only transformer than GPT-2.",
      "why_important": "LLaMA is the de-facto recipe behind most open LLMs (Mistral, Qwen, DeepSeek). Reproducing the modern stack teaches what changed from GPT-2 and why.",
      "pytorch_hook": "RMSNorm + RotaryEmbedding + GQA attention + SwiGLU + residual blocks + LM head"
    },
    {
      "title": "Switch Transformers: Scaling to Trillion Parameter Models with Simple and Efficient Sparsity",
      "authors": "Fedus, Zoph, Shazeer",
      "year": 2021,
      "venue": "JMLR 2022",
      "area": "llm",
      "core_idea": "Replace the dense MLP in every other transformer block with a sparsely-gated mixture of experts: a router sends each token to its top-k experts and the outputs are combined with router weights.",
      "why_important": "MoE is how modern frontier models (GPT-4, Mixtral, DeepSeek-V3) get more parameters without proportionally more FLOPs. Building one teaches routing, load balancing, and sparse dispatch.",
      "pytorch_hook": "TopKRouter + ExpertMLP list + SparseMoE forward + auxiliary load-balancing loss + full LM training loop"
    },
    {
      "title": "Mamba: Linear-Time Sequence Modeling with Selective State Spaces",
      "authors": "Gu and Dao",
      "year": 2023,
      "venue": "arXiv preprint",
      "area": "llm",
      "core_idea": "Replace self-attention with a selective state-space model whose A, B, C matrices depend on the input, giving linear-time sequence modeling with content-based gating.",
      "why_important": "Mamba and its descendants are the first non-attention architectures to compete with transformers at scale and underlie every modern hybrid. Implementing the selective SSM teaches a fundamentally different way to model sequences.",
      "pytorch_hook": "1D causal conv + SiLU gate + selective SSM scan (A, B, C functions of input) + residual blocks + LM head"
    },
    {
      "title": "SAMBA: Simple Hybrid State Space Models for Efficient Unlimited Context Language Modeling",
      "authors": "Ren et al.",
      "year": 2025,
      "venue": "ICLR 2025",
      "area": "llm",
      "core_idea": "Interleave Mamba selective-SSM layers with sliding-window attention layers in a single decoder so the SSM handles long-range compression while local attention handles precise recall.",
      "why_important": "SAMBA scales to 3.8B params with 3.2T tokens and shows 3.7x higher throughput than dense transformers on long context. It is the cleanest recent recipe for hybrid LLMs and a great way to see why both halves matter.",
      "pytorch_hook": "alternating MambaBlock and SlidingWindowAttention layers + SwiGLU MLP + RMSNorm + LM head"
    },
    {
      "title": "Direct Preference Optimization: Your Language Model is Secretly a Reward Model",
      "authors": "Rafailov et al.",
      "year": 2023,
      "venue": "NeurIPS 2023",
      "area": "llm",
      "core_idea": "Derive a closed-form preference loss from the KL-constrained RLHF objective so you can fine-tune the policy directly on (chosen, rejected) pairs without a separate reward model or PPO.",
      "why_important": "DPO replaced PPO as the default alignment recipe in most open-model pipelines (Zephyr, Tulu, Llama-Instruct). Implementing it end-to-end teaches reference policies, token log-probs, and the entire preference fine-tuning loop.",
      "pytorch_hook": "frozen reference policy + sequence_logprob + -log sigmoid(beta * ((pi_c - pi_r) - (ref_c - ref_r))) + train loop + pair-accuracy eval"
    }
  ];

export const SEED_ASSIGNMENTS = [
    {
      "title": "Vision Transformer (ViT) from scratch",
      "paper_ref": "An Image is Worth 16x16 Words: Transformers for Image Recognition at Scale",
      "area": "llm",
      "difficulty": "medium",
      "estimated_hours": 12,
      "why_now": "This is the big-pipeline warmup: ViT forces you to build every part of a real transformer (patch embed, attention, MLP, residual block, full encoder) plus the surrounding training + evaluation pipeline.",
      "learning_objective": "Implement the full ViT architecture and the end-to-end training/evaluation pipeline so a single python assignment.py call trains it and reports a validation accuracy.",
      "context": "ViT showed that a transformer with no convolutions and no recurrence can match or beat CNNs on image classification when applied to sequences of image patches. You will reproduce the architecture and the pipeline around it at a tiny scale that runs on CPU.",
      "prerequisite_concepts": [
        "Tensor shapes and broadcasting",
        "nn.Module, forward(), and parameter registration",
        "Cross-entropy loss and softmax",
        "Adam/AdamW and basic training loop structure"
      ],
      "concept_ladder": [
        "Split an image into non-overlapping patches and project each patch into a vector",
        "Prepend a learnable [CLS] token and add positional embeddings",
        "Run the token sequence through stacked transformer blocks (multi-head self-attention + MLP + residuals + LayerNorm)",
        "Classify from the [CLS] output and train with cross-entropy",
        "Wrap data, model, optimizer, training and evaluation in one run_pipeline() that returns metrics"
      ],
      "next_30_minutes": "Open assignment.py and implement PatchEmbedding so that a (B, C, H, W) input becomes (B, num_patches, embed_dim). Then re-run python assignment.py and watch the first test fail with a clear shape message.",
      "setup": "CPU is enough. No external dataset download - the tests use a tiny synthetic image dataset you build inside get_dataloaders. Use one file: assignment.py.",
      "tasks": [
        "Task 1: Implement PatchEmbedding, MultiHeadSelfAttention, MLPBlock, and TransformerBlock.",
        "Task 2: Implement the full ViT class (patch embed + [CLS] + pos embed + transformer stack + classifier head).",
        "Task 3: Implement get_dataloaders, train_one_epoch, and evaluate.",
        "Task 4: Implement run_pipeline that ties everything together and returns {train_loss, val_accuracy}."
      ],
      "milestones": [
        {
          "title": "Forward pass returns logits",
          "goal": "A random batch flows through the full ViT and produces (B, num_classes) logits with no NaNs.",
          "checkpoint": "test_vit_forward_shape passes."
        },
        {
          "title": "The architecture can learn",
          "goal": "Cross-entropy loss on a single fixed batch drops sharply over a few steps of AdamW.",
          "checkpoint": "test_vit_overfits_one_batch passes."
        },
        {
          "title": "End-to-end pipeline runs",
          "goal": "run_pipeline() builds data, trains the model, evaluates, and returns metrics.",
          "checkpoint": "test_pipeline_runs_and_reports_metrics passes."
        }
      ],
      "key_pytorch_concepts": [
        "nn.Conv2d as a patch embedder",
        "learnable nn.Parameter tokens and positional embeddings",
        "multi-head attention via reshape + matmul + softmax",
        "residual + LayerNorm transformer blocks",
        "full train/eval pipeline returning metrics"
      ],
      "code_build_guide": "# Project: Vision Transformer (ViT) from scratch\n# Paper: An Image is Worth 16x16 Words: Transformers for Image Recognition at Scale\n# Goal: implement the full ViT architecture AND the end-to-end training pipeline\n# Compute: CPU is enough at the tiny config used by the tests\n# Rule: keep this as ONE file. Do not create vit.py, train.py, tests/, or extra modules.\n# Run when done: python assignment.py\n\nimport torch\nimport torch.nn as nn\nimport torch.nn.functional as F\nfrom torch.utils.data import DataLoader, TensorDataset\n\n\n# =========================\n# STUDENT CODE STARTS HERE\n# =========================\n\n\nclass PatchEmbedding(nn.Module):\n    def __init__(self, image_size, patch_size, in_channels, embed_dim):\n        super().__init__()\n        raise AssertionError(\"TODO: implement PatchEmbedding.__init__\")\n\n    def forward(self, x):\n        raise AssertionError(\"TODO: implement PatchEmbedding.forward\")\n\n\nclass MultiHeadSelfAttention(nn.Module):\n    def __init__(self, embed_dim, num_heads):\n        super().__init__()\n        raise AssertionError(\"TODO: implement MultiHeadSelfAttention.__init__\")\n\n    def forward(self, x):\n        raise AssertionError(\"TODO: implement MultiHeadSelfAttention.forward\")\n\n\nclass MLPBlock(nn.Module):\n    def __init__(self, embed_dim, mlp_ratio):\n        super().__init__()\n        raise AssertionError(\"TODO: implement MLPBlock.__init__\")\n\n    def forward(self, x):\n        raise AssertionError(\"TODO: implement MLPBlock.forward\")\n\n\nclass TransformerBlock(nn.Module):\n    def __init__(self, embed_dim, num_heads, mlp_ratio):\n        super().__init__()\n        raise AssertionError(\"TODO: implement TransformerBlock.__init__\")\n\n    def forward(self, x):\n        raise AssertionError(\"TODO: implement TransformerBlock.forward\")\n\n\nclass ViT(nn.Module):\n    def __init__(self, image_size, patch_size, in_channels, num_classes, embed_dim, depth, num_heads, mlp_ratio):\n        super().__init__()\n        raise AssertionError(\"TODO: implement ViT.__init__\")\n\n    def forward(self, x):\n        raise AssertionError(\"TODO: implement ViT.forward\")\n\n\ndef get_dataloaders(num_train, num_val, image_size, in_channels, num_classes, batch_size):\n    raise AssertionError(\"TODO: implement get_dataloaders\")\n\n\ndef train_one_epoch(model, loader, optimizer, device):\n    raise AssertionError(\"TODO: implement train_one_epoch\")\n\n\ndef evaluate(model, loader, device):\n    raise AssertionError(\"TODO: implement evaluate\")\n\n\ndef run_pipeline():\n    raise AssertionError(\"TODO: implement run_pipeline that wires data, model, optimizer, training and evaluation, and returns a dict with train_loss and val_accuracy\")\n\n\n# =========================\n# STUDENT CODE ENDS HERE\n# TESTS START BELOW\n# =========================\n\n\nTINY_CONFIG = dict(\n    image_size=8,\n    patch_size=4,\n    in_channels=3,\n    num_classes=4,\n    embed_dim=32,\n    depth=2,\n    num_heads=4,\n    mlp_ratio=2,\n)\n\n\ndef test_vit_forward_shape():\n    model = ViT(**TINY_CONFIG)\n    x = torch.randn(2, TINY_CONFIG[\"in_channels\"], TINY_CONFIG[\"image_size\"], TINY_CONFIG[\"image_size\"])\n    logits = model(x)\n    assert logits.shape == (2, TINY_CONFIG[\"num_classes\"]), f\"logits shape was {logits.shape}\"\n    assert torch.isfinite(logits).all(), \"logits contain NaN/inf\"\n\n\ndef test_vit_overfits_one_batch():\n    torch.manual_seed(0)\n    model = ViT(**TINY_CONFIG)\n    x = torch.randn(8, TINY_CONFIG[\"in_channels\"], TINY_CONFIG[\"image_size\"], TINY_CONFIG[\"image_size\"])\n    y = torch.randint(0, TINY_CONFIG[\"num_classes\"], (8,))\n    optimizer = torch.optim.AdamW(model.parameters(), lr=3e-3)\n    initial_loss = F.cross_entropy(model(x), y).item()\n    for _ in range(80):\n        optimizer.zero_grad()\n        loss = F.cross_entropy(model(x), y)\n        loss.backward()\n        optimizer.step()\n    final_loss = F.cross_entropy(model(x), y).item()\n    assert final_loss < 0.5 * initial_loss, (\n        f\"final loss {final_loss:.4f} should be much lower than initial {initial_loss:.4f}\"\n    )\n\n\ndef test_pipeline_runs_and_reports_metrics():\n    torch.manual_seed(0)\n    result = run_pipeline()\n    assert isinstance(result, dict), \"run_pipeline must return a dict\"\n    assert \"val_accuracy\" in result, \"result must contain val_accuracy\"\n    assert \"train_loss\" in result, \"result must contain train_loss\"\n    val_acc = float(result[\"val_accuracy\"])\n    assert 0.0 <= val_acc <= 1.0, f\"val_accuracy out of range: {val_acc}\"\n\n\ndef run_all_tests():\n    print(\"Running ViT pipeline checks...\")\n    test_vit_forward_shape()\n    print(\"1/3 ViT forward shape passed\")\n    test_vit_overfits_one_batch()\n    print(\"2/3 model can overfit a single batch\")\n    test_pipeline_runs_and_reports_metrics()\n    print(\"3/3 end-to-end pipeline returns metrics\")\n    print(\"All checks passed. You can mark this assignment done.\")\n\n\nif __name__ == \"__main__\":\n    run_all_tests()\n"
    },
    {
      "title": "Decoder-only GPT language model from scratch",
      "paper_ref": "Language Models are Unsupervised Multitask Learners",
      "area": "llm",
      "difficulty": "medium",
      "estimated_hours": 10,
      "why_now": "After ViT, GPT-2 is the natural next step: same transformer block but causal, plus the full LM training pipeline (next-token cross-entropy, validation loss).",
      "learning_objective": "Implement the full GPT-2 style decoder LM and the end-to-end pretraining pipeline producing train and validation loss.",
      "context": "GPT-2 is the canonical decoder-only causal transformer. You will rebuild token embeddings, causal self-attention, transformer blocks, an LM head, and a synthetic next-token training pipeline.",
      "prerequisite_concepts": [
        "Causal masking",
        "Cross-entropy on (B*T, V) flat logits",
        "Embeddings and weight tying"
      ],
      "concept_ladder": [
        "Token + position embedding produce a (B, T, D) sequence",
        "Causal self-attention mixes only past tokens",
        "Stacked transformer blocks build a deep model",
        "LM head projects back to vocab and trains with cross-entropy"
      ],
      "next_30_minutes": "Implement TokenEmbedding so (B, T) int ids become (B, T, D) embeddings plus learned position embeddings, then run python assignment.py.",
      "setup": "CPU is enough. Synthetic next-token data inside get_dataloaders.",
      "tasks": [
        "Task 1: TokenEmbedding, CausalSelfAttention (with causal mask), MLPBlock, TransformerBlock.",
        "Task 2: Full GPT model with LM head.",
        "Task 3: get_dataloaders producing (input_ids, target_ids) pairs.",
        "Task 4: train_one_epoch, evaluate, run_pipeline returning train_loss and val_loss."
      ],
      "milestones": [
        {
          "title": "Causal forward pass",
          "goal": "GPT returns (B, T, V) logits with proper causal masking.",
          "checkpoint": "test_gpt_forward_shape passes."
        },
        {
          "title": "Overfits a batch",
          "goal": "Cross-entropy on a fixed batch drops sharply.",
          "checkpoint": "test_gpt_overfits_one_batch passes."
        },
        {
          "title": "Pipeline returns metrics",
          "goal": "run_pipeline trains then evaluates and returns dict.",
          "checkpoint": "test_pipeline_runs_and_reports_metrics passes."
        }
      ],
      "key_pytorch_concepts": [
        "causal mask via torch.tril",
        "B*T flat cross-entropy",
        "weight tying between embedding and LM head (optional)",
        "AdamW training loop"
      ],
      "code_build_guide": "# Project: GPT decoder-only language model from scratch\n# Paper: Attention Is All You Need (Vaswani et al., 2017) + Language Models are Unsupervised Multitask Learners (Radford et al., 2019)\n# Goal: implement the full causal transformer LM architecture AND the end-to-end pretraining pipeline\n# Compute: CPU is enough at the tiny config used by the tests\n# Rule: keep this as ONE file.\n# Run when done: python assignment.py\n\nimport torch\nimport torch.nn as nn\nimport torch.nn.functional as F\nfrom torch.utils.data import DataLoader, TensorDataset\n\n\n# =========================\n# STUDENT CODE STARTS HERE\n# =========================\n\n\nclass TokenEmbedding(nn.Module):\n    def __init__(self, vocab_size, embed_dim, max_seq_len):\n        super().__init__()\n        raise AssertionError(\"TODO: implement TokenEmbedding.__init__\")\n\n    def forward(self, idx):\n        raise AssertionError(\"TODO: implement TokenEmbedding.forward\")\n\n\nclass CausalSelfAttention(nn.Module):\n    def __init__(self, embed_dim, num_heads, max_seq_len):\n        super().__init__()\n        raise AssertionError(\"TODO: implement CausalSelfAttention.__init__\")\n\n    def forward(self, x):\n        raise AssertionError(\"TODO: implement CausalSelfAttention.forward\")\n\n\nclass MLPBlock(nn.Module):\n    def __init__(self, embed_dim, mlp_ratio):\n        super().__init__()\n        raise AssertionError(\"TODO: implement MLPBlock.__init__\")\n\n    def forward(self, x):\n        raise AssertionError(\"TODO: implement MLPBlock.forward\")\n\n\nclass TransformerBlock(nn.Module):\n    def __init__(self, embed_dim, num_heads, mlp_ratio, max_seq_len):\n        super().__init__()\n        raise AssertionError(\"TODO: implement TransformerBlock.__init__\")\n\n    def forward(self, x):\n        raise AssertionError(\"TODO: implement TransformerBlock.forward\")\n\n\nclass GPT(nn.Module):\n    def __init__(self, vocab_size, max_seq_len, embed_dim, depth, num_heads, mlp_ratio):\n        super().__init__()\n        raise AssertionError(\"TODO: implement GPT.__init__\")\n\n    def forward(self, idx):\n        raise AssertionError(\"TODO: implement GPT.forward\")\n\n\ndef get_dataloaders(num_train, num_val, vocab_size, seq_len, batch_size):\n    raise AssertionError(\"TODO: implement get_dataloaders\")\n\n\ndef train_one_epoch(model, loader, optimizer, device):\n    raise AssertionError(\"TODO: implement train_one_epoch\")\n\n\ndef evaluate(model, loader, device):\n    raise AssertionError(\"TODO: implement evaluate\")\n\n\ndef run_pipeline():\n    raise AssertionError(\"TODO: implement run_pipeline that wires data, model, optimizer, training and evaluation, and returns a dict with train_loss and val_loss\")\n\n\n# =========================\n# STUDENT CODE ENDS HERE\n# TESTS START BELOW\n# =========================\n\n\nTINY_CONFIG = dict(\n    vocab_size=64,\n    max_seq_len=16,\n    embed_dim=64,\n    depth=2,\n    num_heads=4,\n    mlp_ratio=2,\n)\n\n\ndef test_gpt_forward_shape():\n    model = GPT(**TINY_CONFIG)\n    idx = torch.randint(0, TINY_CONFIG[\"vocab_size\"], (2, TINY_CONFIG[\"max_seq_len\"]))\n    logits = model(idx)\n    expected = (2, TINY_CONFIG[\"max_seq_len\"], TINY_CONFIG[\"vocab_size\"])\n    assert logits.shape == expected, f\"logits shape was {logits.shape}, expected {expected}\"\n    assert torch.isfinite(logits).all(), \"logits contain NaN/inf\"\n\n\ndef test_gpt_overfits_one_batch():\n    torch.manual_seed(0)\n    model = GPT(**TINY_CONFIG)\n    idx = torch.randint(0, TINY_CONFIG[\"vocab_size\"], (4, TINY_CONFIG[\"max_seq_len\"]))\n    targets = torch.randint(0, TINY_CONFIG[\"vocab_size\"], (4, TINY_CONFIG[\"max_seq_len\"]))\n    optimizer = torch.optim.AdamW(model.parameters(), lr=3e-3)\n    V = TINY_CONFIG[\"vocab_size\"]\n    initial = F.cross_entropy(model(idx).reshape(-1, V), targets.reshape(-1)).item()\n    for _ in range(80):\n        optimizer.zero_grad()\n        loss = F.cross_entropy(model(idx).reshape(-1, V), targets.reshape(-1))\n        loss.backward()\n        optimizer.step()\n    final = F.cross_entropy(model(idx).reshape(-1, V), targets.reshape(-1)).item()\n    assert final < 0.5 * initial, f\"final loss {final:.4f} should be much lower than initial {initial:.4f}\"\n\n\ndef test_pipeline_runs_and_reports_metrics():\n    torch.manual_seed(0)\n    result = run_pipeline()\n    assert isinstance(result, dict), \"run_pipeline must return a dict\"\n    assert \"train_loss\" in result and \"val_loss\" in result, \"result must contain train_loss and val_loss\"\n    val_loss = float(result[\"val_loss\"])\n    assert val_loss == val_loss, \"val_loss is NaN\"\n\n\ndef run_all_tests():\n    print(\"Running GPT pipeline checks...\")\n    test_gpt_forward_shape()\n    print(\"1/3 GPT forward shape passed\")\n    test_gpt_overfits_one_batch()\n    print(\"2/3 model can overfit a single batch\")\n    test_pipeline_runs_and_reports_metrics()\n    print(\"3/3 end-to-end pipeline returns metrics\")\n    print(\"All checks passed. You can mark this assignment done.\")\n\n\nif __name__ == \"__main__\":\n    run_all_tests()\n"
    },
    {
      "title": "LLaMA-style modern decoder (RoPE + RMSNorm + SwiGLU + GQA)",
      "paper_ref": "LLaMA: Open and Efficient Foundation Language Models",
      "area": "llm",
      "difficulty": "hard",
      "estimated_hours": 14,
      "why_now": "Once you have GPT-2 working, LLaMA shows what every modern open LLM actually uses: rotary positions, RMSNorm pre-normalization, SwiGLU MLPs, and grouped-query attention.",
      "learning_objective": "Implement the modern LLaMA decoder stack from scratch and train it end-to-end on synthetic data.",
      "context": "LLaMA is the recipe shared by Mistral, Qwen, DeepSeek, and most other open models. Every change vs GPT-2 has a measurable reason; building it from scratch lets you feel them.",
      "prerequisite_concepts": [
        "RoPE rotation of Q/K",
        "RMSNorm vs LayerNorm",
        "SwiGLU activation",
        "Grouped-query attention"
      ],
      "concept_ladder": [
        "RMSNorm normalizes by RMS, no mean subtraction",
        "RotaryEmbedding produces cos/sin tables; apply_rotary rotates Q/K pairs",
        "Grouped-query attention shares K/V across multiple Q heads",
        "SwiGLU = (silu(W1 x) * W2 x) W3",
        "Stack LlamaBlocks and finish with an LM head"
      ],
      "next_30_minutes": "Implement RMSNorm (forward = x * rsqrt(mean(x^2) + eps) * weight) and run python assignment.py once to confirm it imports.",
      "setup": "CPU is enough at the tiny config. Synthetic vocab + sequence data.",
      "tasks": [
        "Task 1: RMSNorm and RotaryEmbedding (cos/sin tables of shape (seq_len, head_dim)).",
        "Task 2: LlamaAttention with GQA and RoPE applied to Q and K.",
        "Task 3: SwiGLU MLP and the full LlamaBlock (pre-norm RMSNorm + attention + RMSNorm + SwiGLU + residuals).",
        "Task 4: LlamaModel stacking blocks; pipeline returns train_loss and val_loss."
      ],
      "milestones": [
        {
          "title": "Forward shape",
          "goal": "LlamaModel returns (B, T, V) logits.",
          "checkpoint": "test_llama_forward_shape passes."
        },
        {
          "title": "Trains on one batch",
          "goal": "Loss halves on a fixed batch within 80 AdamW steps.",
          "checkpoint": "test_llama_overfits_one_batch passes."
        },
        {
          "title": "Pipeline runs",
          "goal": "run_pipeline returns finite train_loss and val_loss.",
          "checkpoint": "test_pipeline_runs_and_reports_metrics passes."
        }
      ],
      "key_pytorch_concepts": [
        "rotary position embedding",
        "RMSNorm",
        "grouped-query attention via repeat_interleave",
        "SwiGLU",
        "pre-norm transformer blocks"
      ],
      "code_build_guide": "# Project: LLaMA-style decoder LM from scratch (RoPE + RMSNorm + SwiGLU + GQA)\n# Paper: LLaMA: Open and Efficient Foundation Language Models (Touvron et al., 2023)\n# Goal: implement the full modern LLaMA architecture AND the end-to-end pretraining pipeline\n# Compute: CPU is enough at the tiny config used by the tests\n# Rule: keep this as ONE file.\n# Run when done: python assignment.py\n\nimport torch\nimport torch.nn as nn\nimport torch.nn.functional as F\nfrom torch.utils.data import DataLoader, TensorDataset\n\n\n# =========================\n# STUDENT CODE STARTS HERE\n# =========================\n\n\nclass RMSNorm(nn.Module):\n    def __init__(self, dim, eps=1e-6):\n        super().__init__()\n        raise AssertionError(\"TODO: implement RMSNorm.__init__\")\n\n    def forward(self, x):\n        raise AssertionError(\"TODO: implement RMSNorm.forward\")\n\n\nclass RotaryEmbedding(nn.Module):\n    def __init__(self, head_dim, max_seq_len, base=10000.0):\n        super().__init__()\n        raise AssertionError(\"TODO: implement RotaryEmbedding.__init__\")\n\n    def forward(self, seq_len):\n        raise AssertionError(\"TODO: implement RotaryEmbedding.forward\")\n\n\nclass LlamaAttention(nn.Module):\n    def __init__(self, embed_dim, num_heads, num_kv_heads, max_seq_len):\n        super().__init__()\n        raise AssertionError(\"TODO: implement LlamaAttention.__init__\")\n\n    def forward(self, x, rope):\n        raise AssertionError(\"TODO: implement LlamaAttention.forward\")\n\n\nclass SwiGLU(nn.Module):\n    def __init__(self, embed_dim, mlp_dim):\n        super().__init__()\n        raise AssertionError(\"TODO: implement SwiGLU.__init__\")\n\n    def forward(self, x):\n        raise AssertionError(\"TODO: implement SwiGLU.forward\")\n\n\nclass LlamaBlock(nn.Module):\n    def __init__(self, embed_dim, num_heads, num_kv_heads, mlp_dim, max_seq_len):\n        super().__init__()\n        raise AssertionError(\"TODO: implement LlamaBlock.__init__\")\n\n    def forward(self, x, rope):\n        raise AssertionError(\"TODO: implement LlamaBlock.forward\")\n\n\nclass LlamaModel(nn.Module):\n    def __init__(self, vocab_size, max_seq_len, embed_dim, depth, num_heads, num_kv_heads, mlp_dim):\n        super().__init__()\n        raise AssertionError(\"TODO: implement LlamaModel.__init__\")\n\n    def forward(self, idx):\n        raise AssertionError(\"TODO: implement LlamaModel.forward\")\n\n\ndef get_dataloaders(num_train, num_val, vocab_size, seq_len, batch_size):\n    raise AssertionError(\"TODO: implement get_dataloaders\")\n\n\ndef train_one_epoch(model, loader, optimizer, device):\n    raise AssertionError(\"TODO: implement train_one_epoch\")\n\n\ndef evaluate(model, loader, device):\n    raise AssertionError(\"TODO: implement evaluate\")\n\n\ndef run_pipeline():\n    raise AssertionError(\"TODO: implement run_pipeline that wires data, model, optimizer, training and evaluation, and returns a dict with train_loss and val_loss\")\n\n\n# =========================\n# STUDENT CODE ENDS HERE\n# TESTS START BELOW\n# =========================\n\n\nTINY_CONFIG = dict(\n    vocab_size=64,\n    max_seq_len=16,\n    embed_dim=64,\n    depth=2,\n    num_heads=4,\n    num_kv_heads=2,\n    mlp_dim=128,\n)\n\n\ndef test_llama_forward_shape():\n    model = LlamaModel(**TINY_CONFIG)\n    idx = torch.randint(0, TINY_CONFIG[\"vocab_size\"], (2, TINY_CONFIG[\"max_seq_len\"]))\n    logits = model(idx)\n    expected = (2, TINY_CONFIG[\"max_seq_len\"], TINY_CONFIG[\"vocab_size\"])\n    assert logits.shape == expected, f\"logits shape was {logits.shape}, expected {expected}\"\n    assert torch.isfinite(logits).all(), \"logits contain NaN/inf\"\n\n\ndef test_llama_overfits_one_batch():\n    torch.manual_seed(0)\n    model = LlamaModel(**TINY_CONFIG)\n    idx = torch.randint(0, TINY_CONFIG[\"vocab_size\"], (4, TINY_CONFIG[\"max_seq_len\"]))\n    targets = torch.randint(0, TINY_CONFIG[\"vocab_size\"], (4, TINY_CONFIG[\"max_seq_len\"]))\n    optimizer = torch.optim.AdamW(model.parameters(), lr=3e-3)\n    V = TINY_CONFIG[\"vocab_size\"]\n    initial = F.cross_entropy(model(idx).reshape(-1, V), targets.reshape(-1)).item()\n    for _ in range(80):\n        optimizer.zero_grad()\n        loss = F.cross_entropy(model(idx).reshape(-1, V), targets.reshape(-1))\n        loss.backward()\n        optimizer.step()\n    final = F.cross_entropy(model(idx).reshape(-1, V), targets.reshape(-1)).item()\n    assert final < 0.5 * initial, f\"final loss {final:.4f} should be much lower than initial {initial:.4f}\"\n\n\ndef test_pipeline_runs_and_reports_metrics():\n    torch.manual_seed(0)\n    result = run_pipeline()\n    assert isinstance(result, dict), \"run_pipeline must return a dict\"\n    assert \"train_loss\" in result and \"val_loss\" in result, \"result must contain train_loss and val_loss\"\n    val_loss = float(result[\"val_loss\"])\n    assert val_loss == val_loss, \"val_loss is NaN\"\n\n\ndef run_all_tests():\n    print(\"Running LLaMA pipeline checks...\")\n    test_llama_forward_shape()\n    print(\"1/3 LLaMA forward shape passed\")\n    test_llama_overfits_one_batch()\n    print(\"2/3 model can overfit a single batch\")\n    test_pipeline_runs_and_reports_metrics()\n    print(\"3/3 end-to-end pipeline returns metrics\")\n    print(\"All checks passed. You can mark this assignment done.\")\n\n\nif __name__ == \"__main__\":\n    run_all_tests()\n"
    },
    {
      "title": "Mixture-of-Experts (MoE) decoder LM",
      "paper_ref": "Switch Transformers: Scaling to Trillion Parameter Models with Simple and Efficient Sparsity",
      "area": "llm",
      "difficulty": "hard",
      "estimated_hours": 14,
      "why_now": "MoE is the architectural lever behind every frontier model: more params, same FLOPs. Building one teaches routing, top-k dispatch, and load balancing.",
      "learning_objective": "Implement the full sparse MoE transformer LM and train it end-to-end with an auxiliary load-balancing loss.",
      "context": "A Switch/Mixtral-style block replaces the dense MLP with N experts and a router that sends each token to its top-k. Routed outputs are weighted by router probabilities. Without a balance loss the router collapses to one expert; with it, all experts get used.",
      "prerequisite_concepts": [
        "Softmax routing",
        "torch.topk and torch.scatter",
        "Auxiliary load-balancing loss"
      ],
      "concept_ladder": [
        "Router projects tokens to N expert logits and selects top-k",
        "ExpertMLP is the same as a vanilla MLP block",
        "SparseMoE dispatches each token to its k experts and weights outputs",
        "Track router stats and add a balance loss to keep experts utilised"
      ],
      "next_30_minutes": "Implement TopKRouter: softmax over expert logits, return (top_k_probs, top_k_indices) shaped (B*T, k). Run python assignment.py.",
      "setup": "CPU is enough. Synthetic next-token sequences.",
      "tasks": [
        "Task 1: TopKRouter and ExpertMLP.",
        "Task 2: SparseMoE forward that dispatches tokens, runs each expert, and combines outputs.",
        "Task 3: MoETransformerBlock with attention + SparseMoE.",
        "Task 4: MoELM stacking blocks. train_one_epoch should optionally add a load-balancing loss term."
      ],
      "milestones": [
        {
          "title": "Sparse forward",
          "goal": "MoELM returns (B, T, V) logits using only top-k experts per token.",
          "checkpoint": "test_moe_forward_shape passes."
        },
        {
          "title": "Trains on one batch",
          "goal": "Cross-entropy on a fixed batch halves within 100 steps.",
          "checkpoint": "test_moe_overfits_one_batch passes."
        },
        {
          "title": "Pipeline runs",
          "goal": "run_pipeline returns train_loss and val_loss.",
          "checkpoint": "test_pipeline_runs_and_reports_metrics passes."
        }
      ],
      "key_pytorch_concepts": [
        "torch.topk",
        "scatter / index_select dispatch",
        "load-balancing auxiliary loss",
        "softmax routing"
      ],
      "code_build_guide": "# Project: Mixture-of-Experts (MoE) decoder LM from scratch\n# Paper: Switch Transformers: Scaling to Trillion Parameter Models with Simple and Efficient Sparsity (Fedus, Zoph, Shazeer, 2021)\n# Goal: implement the full sparse MoE transformer architecture AND the end-to-end pretraining pipeline with a load-balancing loss\n# Compute: CPU is enough at the tiny config used by the tests\n# Rule: keep this as ONE file.\n# Run when done: python assignment.py\n\nimport torch\nimport torch.nn as nn\nimport torch.nn.functional as F\nfrom torch.utils.data import DataLoader, TensorDataset\n\n\n# =========================\n# STUDENT CODE STARTS HERE\n# =========================\n\n\nclass TokenEmbedding(nn.Module):\n    def __init__(self, vocab_size, embed_dim, max_seq_len):\n        super().__init__()\n        raise AssertionError(\"TODO: implement TokenEmbedding.__init__\")\n\n    def forward(self, idx):\n        raise AssertionError(\"TODO: implement TokenEmbedding.forward\")\n\n\nclass CausalSelfAttention(nn.Module):\n    def __init__(self, embed_dim, num_heads, max_seq_len):\n        super().__init__()\n        raise AssertionError(\"TODO: implement CausalSelfAttention.__init__\")\n\n    def forward(self, x):\n        raise AssertionError(\"TODO: implement CausalSelfAttention.forward\")\n\n\nclass ExpertMLP(nn.Module):\n    def __init__(self, embed_dim, mlp_dim):\n        super().__init__()\n        raise AssertionError(\"TODO: implement ExpertMLP.__init__\")\n\n    def forward(self, x):\n        raise AssertionError(\"TODO: implement ExpertMLP.forward\")\n\n\nclass TopKRouter(nn.Module):\n    def __init__(self, embed_dim, num_experts, top_k):\n        super().__init__()\n        raise AssertionError(\"TODO: implement TopKRouter.__init__\")\n\n    def forward(self, x):\n        raise AssertionError(\"TODO: implement TopKRouter.forward\")\n\n\nclass SparseMoE(nn.Module):\n    def __init__(self, embed_dim, num_experts, top_k, mlp_dim):\n        super().__init__()\n        raise AssertionError(\"TODO: implement SparseMoE.__init__\")\n\n    def forward(self, x):\n        raise AssertionError(\"TODO: implement SparseMoE.forward\")\n\n\nclass MoETransformerBlock(nn.Module):\n    def __init__(self, embed_dim, num_heads, num_experts, top_k, mlp_dim, max_seq_len):\n        super().__init__()\n        raise AssertionError(\"TODO: implement MoETransformerBlock.__init__\")\n\n    def forward(self, x):\n        raise AssertionError(\"TODO: implement MoETransformerBlock.forward\")\n\n\nclass MoELM(nn.Module):\n    def __init__(self, vocab_size, max_seq_len, embed_dim, depth, num_heads, num_experts, top_k, mlp_dim):\n        super().__init__()\n        raise AssertionError(\"TODO: implement MoELM.__init__\")\n\n    def forward(self, idx):\n        raise AssertionError(\"TODO: implement MoELM.forward\")\n\n\ndef get_dataloaders(num_train, num_val, vocab_size, seq_len, batch_size):\n    raise AssertionError(\"TODO: implement get_dataloaders\")\n\n\ndef train_one_epoch(model, loader, optimizer, device):\n    raise AssertionError(\"TODO: implement train_one_epoch\")\n\n\ndef evaluate(model, loader, device):\n    raise AssertionError(\"TODO: implement evaluate\")\n\n\ndef run_pipeline():\n    raise AssertionError(\"TODO: implement run_pipeline that wires data, model, optimizer, training and evaluation, and returns a dict with train_loss and val_loss\")\n\n\n# =========================\n# STUDENT CODE ENDS HERE\n# TESTS START BELOW\n# =========================\n\n\nTINY_CONFIG = dict(\n    vocab_size=64,\n    max_seq_len=16,\n    embed_dim=64,\n    depth=2,\n    num_heads=4,\n    num_experts=4,\n    top_k=2,\n    mlp_dim=128,\n)\n\n\ndef _logits_from(model_out):\n    return model_out[0] if isinstance(model_out, tuple) else model_out\n\n\ndef test_moe_forward_shape():\n    model = MoELM(**TINY_CONFIG)\n    idx = torch.randint(0, TINY_CONFIG[\"vocab_size\"], (2, TINY_CONFIG[\"max_seq_len\"]))\n    logits = _logits_from(model(idx))\n    expected = (2, TINY_CONFIG[\"max_seq_len\"], TINY_CONFIG[\"vocab_size\"])\n    assert logits.shape == expected, f\"logits shape was {logits.shape}, expected {expected}\"\n    assert torch.isfinite(logits).all(), \"logits contain NaN/inf\"\n\n\ndef test_moe_overfits_one_batch():\n    torch.manual_seed(0)\n    model = MoELM(**TINY_CONFIG)\n    idx = torch.randint(0, TINY_CONFIG[\"vocab_size\"], (4, TINY_CONFIG[\"max_seq_len\"]))\n    targets = torch.randint(0, TINY_CONFIG[\"vocab_size\"], (4, TINY_CONFIG[\"max_seq_len\"]))\n    optimizer = torch.optim.AdamW(model.parameters(), lr=3e-3)\n    V = TINY_CONFIG[\"vocab_size\"]\n\n    def ce():\n        logits = _logits_from(model(idx))\n        return F.cross_entropy(logits.reshape(-1, V), targets.reshape(-1))\n\n    initial = ce().item()\n    for _ in range(100):\n        optimizer.zero_grad()\n        loss = ce()\n        loss.backward()\n        optimizer.step()\n    final = ce().item()\n    assert final < 0.5 * initial, f\"final loss {final:.4f} should be much lower than initial {initial:.4f}\"\n\n\ndef test_pipeline_runs_and_reports_metrics():\n    torch.manual_seed(0)\n    result = run_pipeline()\n    assert isinstance(result, dict), \"run_pipeline must return a dict\"\n    assert \"train_loss\" in result and \"val_loss\" in result, \"result must contain train_loss and val_loss\"\n\n\ndef run_all_tests():\n    print(\"Running MoE pipeline checks...\")\n    test_moe_forward_shape()\n    print(\"1/3 MoE forward shape passed\")\n    test_moe_overfits_one_batch()\n    print(\"2/3 model can overfit a single batch\")\n    test_pipeline_runs_and_reports_metrics()\n    print(\"3/3 end-to-end pipeline returns metrics\")\n    print(\"All checks passed. You can mark this assignment done.\")\n\n\nif __name__ == \"__main__\":\n    run_all_tests()\n"
    },
    {
      "title": "Mamba selective SSM language model",
      "paper_ref": "Mamba: Linear-Time Sequence Modeling with Selective State Spaces",
      "area": "llm",
      "difficulty": "hard",
      "estimated_hours": 16,
      "why_now": "A full non-attention architecture. Building Mamba forces you to understand selective scans and why content-dependent SSM dynamics rival attention.",
      "learning_objective": "Implement the full Mamba block, stack it into an LM, and train end-to-end on synthetic next-token data.",
      "context": "Mamba replaces self-attention with a selective state-space scan. The A, B, C matrices are functions of the input (selectivity), which gives it content-based gating without quadratic cost. A small reference implementation runs cleanly on CPU.",
      "prerequisite_concepts": [
        "Linear recurrences",
        "1D causal convolutions",
        "SiLU gating",
        "Discretization of continuous SSMs (zero-order hold)"
      ],
      "concept_ladder": [
        "SelectiveSSM: project x -> dt, B, C; discretize A, B; scan over time",
        "MambaBlock: input projection -> conv1d -> SiLU -> SSM -> gate * SSM_out -> output projection",
        "Stack blocks and end with RMSNorm + LM head",
        "Train with cross-entropy on next-token like a transformer"
      ],
      "next_30_minutes": "Implement the input/output projection and 1D causal conv1d inside MambaBlock (no SSM yet) and run python assignment.py.",
      "setup": "CPU is enough. A simple Python loop SSM scan is fine (no CUDA kernels).",
      "tasks": [
        "Task 1: SelectiveSSM with discretized A, B, C dependent on input.",
        "Task 2: MambaBlock = input proj + conv + SiLU + SSM + gate + output proj.",
        "Task 3: MambaLM stacks blocks, embeds tokens, projects to vocab.",
        "Task 4: Full training pipeline returning train_loss and val_loss."
      ],
      "milestones": [
        {
          "title": "SSM forward",
          "goal": "MambaLM returns (B, T, V) logits.",
          "checkpoint": "test_mamba_forward_shape passes."
        },
        {
          "title": "Overfits a batch",
          "goal": "Loss halves on a fixed batch within 120 steps.",
          "checkpoint": "test_mamba_overfits_one_batch passes."
        },
        {
          "title": "Pipeline runs",
          "goal": "run_pipeline returns train_loss and val_loss.",
          "checkpoint": "test_pipeline_runs_and_reports_metrics passes."
        }
      ],
      "key_pytorch_concepts": [
        "1D causal conv",
        "SiLU gating",
        "discretized linear scan",
        "selective parameters as functions of input"
      ],
      "code_build_guide": "# Project: Mamba selective SSM language model from scratch\n# Paper: Mamba: Linear-Time Sequence Modeling with Selective State Spaces (Gu and Dao, 2023)\n# Goal: implement the full Mamba SSM architecture AND the end-to-end pretraining pipeline\n# Compute: CPU is enough at the tiny config used by the tests\n# Rule: keep this as ONE file.\n# Run when done: python assignment.py\n\nimport torch\nimport torch.nn as nn\nimport torch.nn.functional as F\nfrom torch.utils.data import DataLoader, TensorDataset\n\n\n# =========================\n# STUDENT CODE STARTS HERE\n# =========================\n\n\nclass SelectiveSSM(nn.Module):\n    def __init__(self, d_inner, d_state, d_conv):\n        super().__init__()\n        raise AssertionError(\"TODO: implement SelectiveSSM.__init__\")\n\n    def forward(self, x):\n        raise AssertionError(\"TODO: implement SelectiveSSM.forward\")\n\n\nclass MambaBlock(nn.Module):\n    def __init__(self, d_model, d_state, d_conv, expand):\n        super().__init__()\n        raise AssertionError(\"TODO: implement MambaBlock.__init__\")\n\n    def forward(self, x):\n        raise AssertionError(\"TODO: implement MambaBlock.forward\")\n\n\nclass MambaLM(nn.Module):\n    def __init__(self, vocab_size, max_seq_len, d_model, depth, d_state, d_conv, expand):\n        super().__init__()\n        raise AssertionError(\"TODO: implement MambaLM.__init__\")\n\n    def forward(self, idx):\n        raise AssertionError(\"TODO: implement MambaLM.forward\")\n\n\ndef get_dataloaders(num_train, num_val, vocab_size, seq_len, batch_size):\n    raise AssertionError(\"TODO: implement get_dataloaders\")\n\n\ndef train_one_epoch(model, loader, optimizer, device):\n    raise AssertionError(\"TODO: implement train_one_epoch\")\n\n\ndef evaluate(model, loader, device):\n    raise AssertionError(\"TODO: implement evaluate\")\n\n\ndef run_pipeline():\n    raise AssertionError(\"TODO: implement run_pipeline that wires data, model, optimizer, training and evaluation, and returns a dict with train_loss and val_loss\")\n\n\n# =========================\n# STUDENT CODE ENDS HERE\n# TESTS START BELOW\n# =========================\n\n\nTINY_CONFIG = dict(\n    vocab_size=64,\n    max_seq_len=16,\n    d_model=64,\n    depth=2,\n    d_state=8,\n    d_conv=4,\n    expand=2,\n)\n\n\ndef test_mamba_forward_shape():\n    model = MambaLM(**TINY_CONFIG)\n    idx = torch.randint(0, TINY_CONFIG[\"vocab_size\"], (2, TINY_CONFIG[\"max_seq_len\"]))\n    logits = model(idx)\n    expected = (2, TINY_CONFIG[\"max_seq_len\"], TINY_CONFIG[\"vocab_size\"])\n    assert logits.shape == expected, f\"logits shape was {logits.shape}, expected {expected}\"\n    assert torch.isfinite(logits).all(), \"logits contain NaN/inf\"\n\n\ndef test_mamba_overfits_one_batch():\n    torch.manual_seed(0)\n    model = MambaLM(**TINY_CONFIG)\n    idx = torch.randint(0, TINY_CONFIG[\"vocab_size\"], (4, TINY_CONFIG[\"max_seq_len\"]))\n    targets = torch.randint(0, TINY_CONFIG[\"vocab_size\"], (4, TINY_CONFIG[\"max_seq_len\"]))\n    optimizer = torch.optim.AdamW(model.parameters(), lr=3e-3)\n    V = TINY_CONFIG[\"vocab_size\"]\n    initial = F.cross_entropy(model(idx).reshape(-1, V), targets.reshape(-1)).item()\n    for _ in range(120):\n        optimizer.zero_grad()\n        loss = F.cross_entropy(model(idx).reshape(-1, V), targets.reshape(-1))\n        loss.backward()\n        optimizer.step()\n    final = F.cross_entropy(model(idx).reshape(-1, V), targets.reshape(-1)).item()\n    assert final < 0.5 * initial, f\"final loss {final:.4f} should be much lower than initial {initial:.4f}\"\n\n\ndef test_pipeline_runs_and_reports_metrics():\n    torch.manual_seed(0)\n    result = run_pipeline()\n    assert isinstance(result, dict), \"run_pipeline must return a dict\"\n    assert \"train_loss\" in result and \"val_loss\" in result, \"result must contain train_loss and val_loss\"\n\n\ndef run_all_tests():\n    print(\"Running Mamba pipeline checks...\")\n    test_mamba_forward_shape()\n    print(\"1/3 Mamba forward shape passed\")\n    test_mamba_overfits_one_batch()\n    print(\"2/3 model can overfit a single batch\")\n    test_pipeline_runs_and_reports_metrics()\n    print(\"3/3 end-to-end pipeline returns metrics\")\n    print(\"All checks passed. You can mark this assignment done.\")\n\n\nif __name__ == \"__main__\":\n    run_all_tests()\n"
    },
    {
      "title": "SAMBA hybrid LM (Mamba + sliding-window attention)",
      "paper_ref": "SAMBA: Simple Hybrid State Space Models for Efficient Unlimited Context Language Modeling",
      "area": "llm",
      "difficulty": "hard",
      "estimated_hours": 16,
      "why_now": "After plain Mamba, build the 2025 hybrid that interleaves Mamba and sliding-window attention. This is the cleanest recent recipe for combining SSMs with attention.",
      "learning_objective": "Implement a SAMBA-style decoder that alternates Mamba blocks with sliding-window attention blocks and train it end-to-end.",
      "context": "SAMBA shows that attention and SSMs are complementary: SSM compresses long history, sliding-window attention does precise local recall. The architecture stacks both kinds of layers in alternation, with SwiGLU MLPs.",
      "prerequisite_concepts": [
        "Selective SSMs (from the Mamba assignment)",
        "Sliding-window attention masks",
        "Pre-norm residual blocks"
      ],
      "concept_ladder": [
        "Reuse the SelectiveSSM and MambaBlock you already wrote",
        "Add SlidingWindowAttention with a local causal mask of width w",
        "SambaLayer wraps either a Mamba or SWA op plus a SwiGLU MLP with residuals",
        "SambaLM interleaves the two layer kinds in a single stack"
      ],
      "next_30_minutes": "Sketch SlidingWindowAttention: causal mask where position i can only see positions in [i - window + 1, i].",
      "setup": "CPU is enough. Synthetic next-token data, seq_len=32, window_size=8.",
      "tasks": [
        "Task 1: SelectiveSSM + MambaBlock (reuse your Mamba assignment).",
        "Task 2: SlidingWindowAttention with local causal mask.",
        "Task 3: SwiGLU MLP and SambaLayer (kind in {'mamba', 'swa'}).",
        "Task 4: SambaLM that interleaves layer kinds; full training pipeline."
      ],
      "milestones": [
        {
          "title": "Hybrid forward",
          "goal": "SambaLM returns (B, T, V) logits with both layer kinds in the stack.",
          "checkpoint": "test_samba_forward_shape passes."
        },
        {
          "title": "Trains on one batch",
          "goal": "Loss halves on a fixed batch.",
          "checkpoint": "test_samba_overfits_one_batch passes."
        },
        {
          "title": "Pipeline runs",
          "goal": "run_pipeline returns train_loss and val_loss.",
          "checkpoint": "test_pipeline_runs_and_reports_metrics passes."
        }
      ],
      "key_pytorch_concepts": [
        "sliding-window causal mask",
        "alternating layer kinds in a stack",
        "Mamba SSM + attention in one architecture",
        "SwiGLU + RMSNorm"
      ],
      "code_build_guide": "# Project: SAMBA hybrid LM from scratch (Mamba + Sliding Window Attention)\n# Paper: SAMBA: Simple Hybrid State Space Models for Efficient Unlimited Context Language Modeling (Ren et al., ICLR 2025)\n# Goal: implement the full hybrid Mamba + sliding-window attention architecture AND the end-to-end pretraining pipeline\n# Compute: CPU is enough at the tiny config used by the tests\n# Rule: keep this as ONE file.\n# Run when done: python assignment.py\n\nimport torch\nimport torch.nn as nn\nimport torch.nn.functional as F\nfrom torch.utils.data import DataLoader, TensorDataset\n\n\n# =========================\n# STUDENT CODE STARTS HERE\n# =========================\n\n\nclass SelectiveSSM(nn.Module):\n    def __init__(self, d_inner, d_state, d_conv):\n        super().__init__()\n        raise AssertionError(\"TODO: implement SelectiveSSM.__init__\")\n\n    def forward(self, x):\n        raise AssertionError(\"TODO: implement SelectiveSSM.forward\")\n\n\nclass MambaBlock(nn.Module):\n    def __init__(self, d_model, d_state, d_conv, expand):\n        super().__init__()\n        raise AssertionError(\"TODO: implement MambaBlock.__init__\")\n\n    def forward(self, x):\n        raise AssertionError(\"TODO: implement MambaBlock.forward\")\n\n\nclass SlidingWindowAttention(nn.Module):\n    def __init__(self, embed_dim, num_heads, window_size):\n        super().__init__()\n        raise AssertionError(\"TODO: implement SlidingWindowAttention.__init__\")\n\n    def forward(self, x):\n        raise AssertionError(\"TODO: implement SlidingWindowAttention.forward\")\n\n\nclass SwiGLU(nn.Module):\n    def __init__(self, embed_dim, mlp_dim):\n        super().__init__()\n        raise AssertionError(\"TODO: implement SwiGLU.__init__\")\n\n    def forward(self, x):\n        raise AssertionError(\"TODO: implement SwiGLU.forward\")\n\n\nclass SambaLayer(nn.Module):\n    def __init__(self, d_model, d_state, d_conv, expand, num_heads, window_size, mlp_dim, layer_kind):\n        super().__init__()\n        raise AssertionError(\"TODO: implement SambaLayer.__init__ ; layer_kind is 'mamba' or 'swa'\")\n\n    def forward(self, x):\n        raise AssertionError(\"TODO: implement SambaLayer.forward\")\n\n\nclass SambaLM(nn.Module):\n    def __init__(self, vocab_size, max_seq_len, d_model, depth, d_state, d_conv, expand, num_heads, window_size, mlp_dim):\n        super().__init__()\n        raise AssertionError(\"TODO: implement SambaLM.__init__ that interleaves Mamba and SWA layers\")\n\n    def forward(self, idx):\n        raise AssertionError(\"TODO: implement SambaLM.forward\")\n\n\ndef get_dataloaders(num_train, num_val, vocab_size, seq_len, batch_size):\n    raise AssertionError(\"TODO: implement get_dataloaders\")\n\n\ndef train_one_epoch(model, loader, optimizer, device):\n    raise AssertionError(\"TODO: implement train_one_epoch\")\n\n\ndef evaluate(model, loader, device):\n    raise AssertionError(\"TODO: implement evaluate\")\n\n\ndef run_pipeline():\n    raise AssertionError(\"TODO: implement run_pipeline that wires data, model, optimizer, training and evaluation, and returns a dict with train_loss and val_loss\")\n\n\n# =========================\n# STUDENT CODE ENDS HERE\n# TESTS START BELOW\n# =========================\n\n\nTINY_CONFIG = dict(\n    vocab_size=64,\n    max_seq_len=32,\n    d_model=64,\n    depth=4,\n    d_state=8,\n    d_conv=4,\n    expand=2,\n    num_heads=4,\n    window_size=8,\n    mlp_dim=128,\n)\n\n\ndef test_samba_forward_shape():\n    model = SambaLM(**TINY_CONFIG)\n    idx = torch.randint(0, TINY_CONFIG[\"vocab_size\"], (2, TINY_CONFIG[\"max_seq_len\"]))\n    logits = model(idx)\n    expected = (2, TINY_CONFIG[\"max_seq_len\"], TINY_CONFIG[\"vocab_size\"])\n    assert logits.shape == expected, f\"logits shape was {logits.shape}, expected {expected}\"\n    assert torch.isfinite(logits).all(), \"logits contain NaN/inf\"\n\n\ndef test_samba_overfits_one_batch():\n    torch.manual_seed(0)\n    model = SambaLM(**TINY_CONFIG)\n    idx = torch.randint(0, TINY_CONFIG[\"vocab_size\"], (4, TINY_CONFIG[\"max_seq_len\"]))\n    targets = torch.randint(0, TINY_CONFIG[\"vocab_size\"], (4, TINY_CONFIG[\"max_seq_len\"]))\n    optimizer = torch.optim.AdamW(model.parameters(), lr=3e-3)\n    V = TINY_CONFIG[\"vocab_size\"]\n    initial = F.cross_entropy(model(idx).reshape(-1, V), targets.reshape(-1)).item()\n    for _ in range(120):\n        optimizer.zero_grad()\n        loss = F.cross_entropy(model(idx).reshape(-1, V), targets.reshape(-1))\n        loss.backward()\n        optimizer.step()\n    final = F.cross_entropy(model(idx).reshape(-1, V), targets.reshape(-1)).item()\n    assert final < 0.5 * initial, f\"final loss {final:.4f} should be much lower than initial {initial:.4f}\"\n\n\ndef test_pipeline_runs_and_reports_metrics():\n    torch.manual_seed(0)\n    result = run_pipeline()\n    assert isinstance(result, dict), \"run_pipeline must return a dict\"\n    assert \"train_loss\" in result and \"val_loss\" in result, \"result must contain train_loss and val_loss\"\n\n\ndef run_all_tests():\n    print(\"Running SAMBA hybrid pipeline checks...\")\n    test_samba_forward_shape()\n    print(\"1/3 SAMBA forward shape passed\")\n    test_samba_overfits_one_batch()\n    print(\"2/3 hybrid model can overfit a single batch\")\n    test_pipeline_runs_and_reports_metrics()\n    print(\"3/3 end-to-end pipeline returns metrics\")\n    print(\"All checks passed. You can mark this assignment done.\")\n\n\nif __name__ == \"__main__\":\n    run_all_tests()\n"
    },
    {
      "title": "DPO alignment pipeline from scratch",
      "paper_ref": "Direct Preference Optimization: Your Language Model is Secretly a Reward Model",
      "area": "llm",
      "difficulty": "medium",
      "estimated_hours": 10,
      "why_now": "Alignment is the other half of LLM work. DPO is the canonical preference-fine-tuning recipe; doing it end-to-end teaches reference policies, token log-probs, and pair evaluation.",
      "learning_objective": "Implement a tiny LM plus the full DPO training pipeline against a frozen reference, and evaluate pair accuracy.",
      "context": "DPO derives a preference loss directly from the KL-constrained RLHF objective: -log sigmoid(beta * ((pi_chosen - pi_rejected) - (ref_chosen - ref_rejected))). No reward model, no PPO. You will build the tiny LM, the loss, the training loop, and a pair-accuracy evaluator.",
      "prerequisite_concepts": [
        "Token log-probabilities",
        "Frozen reference models",
        "Bradley-Terry preference modeling"
      ],
      "concept_ladder": [
        "Build a small causal transformer LM (TinyLM)",
        "sequence_logprob computes log p(completion | prompt) under the policy",
        "dpo_loss combines policy and reference logprobs with beta",
        "Training updates only the policy; the reference stays frozen",
        "Evaluate pair_accuracy = fraction of pairs where policy prefers chosen"
      ],
      "next_30_minutes": "Build TinyLM (same shape as your GPT) and confirm logits shape with python assignment.py.",
      "setup": "CPU is enough. Synthetic (chosen, rejected) preference pairs from get_dataloaders.",
      "tasks": [
        "Task 1: TinyLM (token embed + causal blocks + LM head).",
        "Task 2: sequence_logprob for completion tokens only (positions >= prompt_len).",
        "Task 3: dpo_loss against a frozen reference with beta=0.1.",
        "Task 4: train_one_epoch + evaluate + run_pipeline returning train_loss and pair_accuracy."
      ],
      "milestones": [
        {
          "title": "TinyLM forward",
          "goal": "Logits shape correct on a batch.",
          "checkpoint": "test_tinylm_forward_shape passes."
        },
        {
          "title": "DPO loss decreases",
          "goal": "AdamW + dpo_loss decrease on a fixed pair batch.",
          "checkpoint": "test_dpo_loss_decreases_on_one_batch passes."
        },
        {
          "title": "Pipeline runs",
          "goal": "run_pipeline returns train_loss and pair_accuracy in [0, 1].",
          "checkpoint": "test_pipeline_runs_and_reports_metrics passes."
        }
      ],
      "key_pytorch_concepts": [
        "torch.gather on log-softmax logits",
        "requires_grad_(False) for the reference",
        "sigmoid + log of preference margin",
        "pair-accuracy evaluation"
      ],
      "code_build_guide": "# Project: DPO alignment pipeline from scratch\n# Paper: Direct Preference Optimization: Your Language Model is Secretly a Reward Model (Rafailov et al., NeurIPS 2023)\n# Goal: implement a tiny LM plus the FULL alignment pipeline (preference data, DPO training with a frozen reference, evaluation)\n# Compute: CPU is enough at the tiny config used by the tests\n# Rule: keep this as ONE file.\n# Run when done: python assignment.py\n\nimport copy\nimport torch\nimport torch.nn as nn\nimport torch.nn.functional as F\nfrom torch.utils.data import DataLoader, TensorDataset\n\n\n# =========================\n# STUDENT CODE STARTS HERE\n# =========================\n\n\nclass TokenEmbedding(nn.Module):\n    def __init__(self, vocab_size, embed_dim, max_seq_len):\n        super().__init__()\n        raise AssertionError(\"TODO: implement TokenEmbedding.__init__\")\n\n    def forward(self, idx):\n        raise AssertionError(\"TODO: implement TokenEmbedding.forward\")\n\n\nclass CausalSelfAttention(nn.Module):\n    def __init__(self, embed_dim, num_heads, max_seq_len):\n        super().__init__()\n        raise AssertionError(\"TODO: implement CausalSelfAttention.__init__\")\n\n    def forward(self, x):\n        raise AssertionError(\"TODO: implement CausalSelfAttention.forward\")\n\n\nclass MLPBlock(nn.Module):\n    def __init__(self, embed_dim, mlp_ratio):\n        super().__init__()\n        raise AssertionError(\"TODO: implement MLPBlock.__init__\")\n\n    def forward(self, x):\n        raise AssertionError(\"TODO: implement MLPBlock.forward\")\n\n\nclass TransformerBlock(nn.Module):\n    def __init__(self, embed_dim, num_heads, mlp_ratio, max_seq_len):\n        super().__init__()\n        raise AssertionError(\"TODO: implement TransformerBlock.__init__\")\n\n    def forward(self, x):\n        raise AssertionError(\"TODO: implement TransformerBlock.forward\")\n\n\nclass TinyLM(nn.Module):\n    def __init__(self, vocab_size, max_seq_len, embed_dim, depth, num_heads, mlp_ratio):\n        super().__init__()\n        raise AssertionError(\"TODO: implement TinyLM.__init__\")\n\n    def forward(self, idx):\n        raise AssertionError(\"TODO: implement TinyLM.forward\")\n\n\ndef sequence_logprob(model, sequence_ids, prompt_len):\n    raise AssertionError(\"TODO: implement sequence_logprob (sum log-prob of completion tokens only, after the prompt)\")\n\n\ndef dpo_loss(policy, reference, chosen_ids, rejected_ids, prompt_len, beta):\n    raise AssertionError(\"TODO: implement dpo_loss using sequence_logprob and the Rafailov objective\")\n\n\ndef get_dataloaders(num_train, num_val, vocab_size, seq_len, prompt_len, batch_size):\n    raise AssertionError(\"TODO: implement get_dataloaders that yields (chosen_ids, rejected_ids)\")\n\n\ndef train_one_epoch(policy, reference, loader, optimizer, device, beta, prompt_len):\n    raise AssertionError(\"TODO: implement train_one_epoch for DPO\")\n\n\ndef evaluate(policy, reference, loader, device, prompt_len):\n    raise AssertionError(\"TODO: implement evaluate returning pair_accuracy (fraction where seq_logprob(chosen) > seq_logprob(rejected) under policy)\")\n\n\ndef run_pipeline():\n    raise AssertionError(\"TODO: implement run_pipeline that builds policy + frozen reference, runs DPO training, evaluates, and returns dict with train_loss and pair_accuracy\")\n\n\n# =========================\n# STUDENT CODE ENDS HERE\n# TESTS START BELOW\n# =========================\n\n\nTINY_CONFIG = dict(\n    vocab_size=32,\n    max_seq_len=16,\n    embed_dim=64,\n    depth=2,\n    num_heads=4,\n    mlp_ratio=2,\n)\nBETA = 0.1\nPROMPT_LEN = 4\n\n\ndef test_tinylm_forward_shape():\n    model = TinyLM(**TINY_CONFIG)\n    idx = torch.randint(0, TINY_CONFIG[\"vocab_size\"], (2, TINY_CONFIG[\"max_seq_len\"]))\n    logits = model(idx)\n    expected = (2, TINY_CONFIG[\"max_seq_len\"], TINY_CONFIG[\"vocab_size\"])\n    assert logits.shape == expected, f\"logits shape was {logits.shape}, expected {expected}\"\n    assert torch.isfinite(logits).all(), \"logits contain NaN/inf\"\n\n\ndef test_dpo_loss_decreases_on_one_batch():\n    torch.manual_seed(0)\n    policy = TinyLM(**TINY_CONFIG)\n    reference = copy.deepcopy(policy)\n    for p in reference.parameters():\n        p.requires_grad_(False)\n    chosen = torch.randint(0, TINY_CONFIG[\"vocab_size\"], (4, TINY_CONFIG[\"max_seq_len\"]))\n    rejected = torch.randint(0, TINY_CONFIG[\"vocab_size\"], (4, TINY_CONFIG[\"max_seq_len\"]))\n    optimizer = torch.optim.AdamW(policy.parameters(), lr=3e-3)\n    initial = dpo_loss(policy, reference, chosen, rejected, PROMPT_LEN, BETA).item()\n    for _ in range(80):\n        optimizer.zero_grad()\n        loss = dpo_loss(policy, reference, chosen, rejected, PROMPT_LEN, BETA)\n        loss.backward()\n        optimizer.step()\n    final = dpo_loss(policy, reference, chosen, rejected, PROMPT_LEN, BETA).item()\n    assert final < initial - 0.05, f\"final dpo loss {final:.4f} should be lower than initial {initial:.4f}\"\n\n\ndef test_pipeline_runs_and_reports_metrics():\n    torch.manual_seed(0)\n    result = run_pipeline()\n    assert isinstance(result, dict), \"run_pipeline must return a dict\"\n    assert \"train_loss\" in result, \"result must contain train_loss\"\n    assert \"pair_accuracy\" in result, \"result must contain pair_accuracy\"\n    pa = float(result[\"pair_accuracy\"])\n    assert 0.0 <= pa <= 1.0, f\"pair_accuracy out of range: {pa}\"\n\n\ndef run_all_tests():\n    print(\"Running DPO pipeline checks...\")\n    test_tinylm_forward_shape()\n    print(\"1/3 TinyLM forward shape passed\")\n    test_dpo_loss_decreases_on_one_batch()\n    print(\"2/3 DPO loss decreases on a fixed batch\")\n    test_pipeline_runs_and_reports_metrics()\n    print(\"3/3 end-to-end pipeline returns metrics\")\n    print(\"All checks passed. You can mark this assignment done.\")\n\n\nif __name__ == \"__main__\":\n    run_all_tests()\n"
    }
  ];
